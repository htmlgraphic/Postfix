machine:
  services:
    - docker
dependencies:
  override:
    - docker build -t postfix Docker/Postfix/
test:
  override:
    # test postfix
    - docker run -d -p 25:25 --name postfix -e USER=p08tf1X -e PASS=p@ssw0Rd -e HOSTNAME=post-office.htmlgraphic.com postfix; sleep 20
    - docker logs postfix | grep "p08tf1X:p@ssw0Rd"