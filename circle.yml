machine:
  services:
    - docker
dependencies:
  override:
    - docker build -t postfix .
test:
  override:
    # test postfix
    - docker run -d -p 25:25 --name postfix -e USER=p08tf1X -e PASS=p@ssw0Rd -e LOG_TOKEN=8397b752-123f-4d10-95b1-76f267102a7b -e HOSTNAME=post-office.htmlgraphic.com postfix; sleep 20
    - docker logs postfix | grep "p08tf1X:p@ssw0Rd"
    - docker logs postfix | grep "8397b752-123f-4d10-95b1-76f267102a7b"
