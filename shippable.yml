language: python

build_image: htmlgraphic/postfix

script:
  - /usr/sbin/postconf -n
  - bash Docker/Postfix/build_tests.sh

notifications:
  email:
    recipients:
      - jason@htmlgraphic.com
    on_success: change
    on_failure: always

branches:
  only:
  - master
  - develop